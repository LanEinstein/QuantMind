# backend/strategy_evolution/ — 子任务上下文(Phase R)

> 状态:**R-001 done(#73)+ R-002 done(#79)+ Phase AB done(2026-06-12,session #80:AB-001..AB-008)**;R-003 sim 范围由 AB 客观晋升替代(实盘人工 gate 保留)、R-004 cron+sub-budget 已被 AB-007 吸收(audit 类5 七类先前已落)。新增治理:[P2-2-amendment-2026-06-12](../../docs/decisions/P2-2-amendment-2026-06-12-sim-objective-promotion.md)(sim 范围客观晋升)。治理:[P2-2-amendment-2026-05-24](../../docs/decisions/P2-2-amendment-2026-05-24-active-discovery-knowledge-graph.md) + R0 §8。任务:plan.html R-001..R-005。

## 职责
**自进化**:agent 发现/验证/淘汰策略;生命周期 `candidate→shadow→active→decaying→retired`;经人工 gate 进化知识库。

## 本模块红线(进化自主度 = 人工 gate,owner 2026-05-24 锁定)
1. **`LiveArtifactRegistry`**:startup 从**不可变 config** 载入批准哈希集 `{strategy_code_hash, feature_def_hash, prompt_version_hash, anomaly_model_hash, rag_index_version}`;实时路径**拒任何不在集内**的哈希;**无 runtime 路径**加哈希。**对抗测试先写(RED)**:种入未批准高 Sharpe 策略 → 实时 selector 不可读/执行;有效但未 pin 也拒。
2. **人工 gate 不变**:发现物经 **45 日 shadow validate**(完全沿用 P0-6 5+3 硬门槛)→ 飞书通知 → 人工起草 amendment + 逐条批准 → git commit + 重启才生效。**agent 永不自动改决策路径/风控/config**。
3. **7 禁不变**:fine-tune / online learning / RLHF / DPO / continual SFT / 新 LLM provider / **LLM 自动决策权**。"主动发现" = 提候选 + 量化验证,**非自动上线**。
4. rqalpha 作 **test-time 差分 oracle**(交叉校验 MockBroker);MockBroker 仍**单一镜像**,rqalpha **永不入实时路径**。de Prado 防过拟(purged CV / deflated Sharpe)。
5. `evolution_shadow_run` 22:00(BrokerScheduler 第五 cron)+ **独立 sub-budget**(日余额低降级/跳过;MVP 阶段 OUT);audit 类 5 七类 actor=SYSTEM/SCHEDULER,**LLM 严禁写 audit**。

## import 隔离
严禁 `import backend.{api,broker,risk,llm,agents,mirofish,data}`(继承 P2-2;防绕过守门)。可用:`backend.knowledge_graph` + `backend.marketdata_snapshot`(回测取数)+ rqalpha。

## 接口契约
- **`LiveArtifactRegistry`(R-001 已实现,`live_artifact_registry.py`)**:`from_lockfile("config/live_artifacts.lock.json")` boot 载入(fail-closed:缺文件/坏 JSON/坏 schema/非 sha256/未知 kind 全 raise)+ `from_lock(lock)`(in-memory/测试)。`is_approved(kind: ArtifactKind, identifier) -> bool` **kind-typed**(草案的 `is_approved(hash)` 精化为 5 类分型,防策略哈希批准 prompt;在已决 5 类边界内,非新 amendment)+ `approved(kind) -> frozenset[str]`。5 类 `ArtifactKind` = strategy_code/feature_def/prompt_version/anomaly_model/rag_index,全 sha256 hex(content-addressed)。**完全不可变**(`__setattr__` raise,无 approve/add/reload/promote;无 runtime 加哈希路径);空 bootstrap = deny-all。晋升经 amendment+pin+git+restart。
- **生命周期(R-002 已实现,`lifecycle.py`)**:`StrategyLifecycleState` 5 态 + `ALLOWED_LIFECYCLE_TRANSITIONS` allowlist(RETIRED 终态,无出边)+ `transition_lifecycle`(唯一转移入口;**ACTIVE 必须 registry pin** —— `is_approved(STRATEGY_CODE, hash)` 不过即 `UnapprovedStrategyError`,无 registry 同拒)+ `MongoLifecycleLedger`(append-only `strategy_lifecycle_events`;current state = fold;**retired 哈希永不可 re-propose**)。
- **差分 oracle(R-002 已实现,`backtest_oracle.py`)**:`BacktestRunner` 注入式 Protocol;`compare_equity_curves` 纯函数(共享日 |diff| ≤25bps、散点日 ≤5% 才 CONSISTENT)+ `run_differential_check`(oracle 失败 → `ORACLE_UNAVAILABLE`,**非 pass、不抛**)。`RqalphaBacktestRunner` lazy import 可选依赖;**rqalpha LICENSE 已读(2026-06-12):Apache 2.0 + 商用需米筐书面授权 —— 永不 vendor、不抄代码、仅非商用 pip 依赖;真实 run harness 归 Phase AB(数据 bundle + Mod 配置),此前恒 UNAVAILABLE fail-closed**。redline `[R-002]` + AST 契约测试钉死 rqalpha 不入实时路径。
- **防过拟(R-002 已实现,`anti_overfit.py`)**:`purged_kfold_splits`(purge+embargo)+ `deflated_sharpe_ratio`(PSR vs E[max SR | N trials],Bailey-de Prado 公式自推导)+ `meets_anti_overfit_bar`(DSR ≥0.95);纯数学零 IO,AB 晋升门消费。
- **ExperimentRegistry(AB-001,`experiment_registry.py`)**:append-only `evolution_experiments`(**含全部失败实验**;同 design 重注册=幂等 skip,outcome 永不可洗);`experiment_id` content-addressed over DESIGN(kind+family+hypothesis+artifact+param_space+window,排除 outcome);`count_trials(family)` 供多重检验校正 + `bonferroni_alpha` 单调收紧。
- **ObjectivePromotionEngine(AB-002,`objective_promotion.py`)**:纯函数 `evaluate_promotion(PromotionInputs) -> PromotionDecision`,9 个命名 gate 全确定性可 replay(分层窗口双门槛 15td/30、20td/15、45td/30 + ShadowChain 锁定 bootstrap CI〔seed 20260518〕low>0 + deflated Sharpe ≥0.95〔n_trials 来自 registry 含失败〕+ 锁定 8 门 evaluate_challenger 不劣化 + anti-gaming 敞口≥10%/信号数/换手带 + oracle DIVERGENT 硬拒、UNAVAILABLE 过但记 `oracle_cross_checked=False`);`inputs_digest` 排除 report 行身份字段。**LLM 零参与**(全数值输入)。
- **PromotionIntent + activation(AB-003,`promotion_intent.py`+`activation.py`)**:intent 唯一构造 `build_promotion_intent`(**feishu 模式 raise PromotionModeError**=实盘人工 gate 不变;PROMOTE 必须 promoted 决策);append-only `promotion_intent_events`(PENDING→ACTIVATED/CANCELLED/FROZEN;FROZEN→CANCELLED only;`freeze_all_pending` = 模式切换钩);`ActivationManifest` content-addressed over 目标态(approved 5 类 + 白名单 params,build 时过 AB-005 校验);`write_next_boot_lock`(原子 + 模式门 + **距开盘≥2h 黑窗拒**);`apply_pending_activation`(boot consume-once:备份→原子换 lockfile→LiveArtifactRegistry 健康断言→**失败自动回退** prev 字节;损坏 staged 隔离 `.bad` 不碰 live);`rollback_to_previous`(AB-004 降级用,备份坏则拒)。**零 git/subprocess**(redline [AB-008] + AST 对抗);受控重启=外部 `deploy/promote_restart.sh`(08:25-08:45 窗 + staged lock 存在才 restart)。
- **auto-demote(AB-004,`demotion.py`)**:`evaluate_demotion` 纯函数(challenger vs incumbent **counterfactual** 同长日 PnL;≥10 日 + 累计相对超额 ≤-150bps 才降级;**相对基线防熊市误杀**;长度不匹配/坏 equity base = 无判定 fail-closed)+ `is_in_promotion_cooldown`(同族 10 日冷却)。降级不碰 broker(持仓沿用 entry 铭牌,AA-004 已钉)。
- **冻结集+白名单(AB-005,`evolvable_params.py`)**:`FROZEN_NON_EVOLVABLE`(安全地基+仓位三连+槽cap+熔断4+预算4+universe 排除4+14-check+对账阈值+模式开关,命中即 raise FrozenParamViolationError 不论分数)+ `EVOLVABLE_WHITELIST`(MappingProxyType,每参数 immutable clamp;safety-adjacent〔能推迟 SELL〕= 更紧 clamp + 止损只紧不松单调;selector 权重 sum=1 组约束;tier 权重序约束;value 槽 0..2 整数域)。
- **prompt 政策化(AB-006,`prompt_policy.py`)**:禁类 lint(带量买卖指令/InstructionPlan 词汇/绕风控/注入标记/直接执行,中英双语 v1 deny-list)+ `PromptByteCapture`(原始字节 sha256;**任一未捕获=non-promotable,0 调用也 non-promotable**)+ 骨架 frozen 顺序校验;GEPA runner 产物出炉即 lint(`GEPAPolicyLintError`),走 AB-002 同一晋升门。
- **harsh fill(AB-007,`harsh_fill_model.py`)**:shadow 评估专用严苛撮合(涨跌停不成交/ADV 5% 容量+整手向下取/线性价格冲击/陈旧报价拒/次日 bar 延迟成交且有利缺口被钳=**harsh-or-equal 契约**);零 broker import(实盘撮合 bit-identical by construction)。22:00 cron 已接线(main.py 回调:`reserve_evolution_run` 双层预算门〔QUANTMIND_EVOLUTION_DAILY_SUBBUDGET 默认 ¥10 + 统一 ¥100 预留,不绕 cap〕→ dispatcher 存在即跑;**当前 ShadowChain 缺 ChallengerReplayer 具体实现 → dispatcher 未挂,audit DEGRADED 留痕,gap 记 plan.html AB-007 notes**)。
- `shadow_validate` 45 日完整 challenger replay(ChallengerReplayer 具体实现)= AB 后续 harness 缺口;实盘模式晋升仍走 P2-2 原人工 gate。
- **Reflexion + exemplars(T-004,`reflexion.py`)**:交易员 agent 行为进化的**离线**半边(人格卡 frozen=「是谁」/exemplars=「好输出」分离,P0-10-amendment-2026-05-24 §2.2)。`curate_exemplars`(确定性:**仅 RiskEngine-passed AND profitable 案例** + AB-006 `lint_prompt_artifact` 过滤带量买卖/订单/注入文本 + FinMem 递减权重 + **≤3 cap** + dedup + 可 replay)→ `ExemplarArtifact`(content-addressed,构造期 fail-closed 校验 ≤3 + 全 lint-pass)→ `propose_persona_card_version`(骨架 frozen 逐字节保留 identity/mandate/output_contract,仅换 version + exemplars,产出新卡 YAML + sha256)。**只 propose 永不自动应用**:新卡 sha256 须经人工 pin 为 `LiveArtifactRegistry.PROMPT_VERSION`(amendment+重启)才 runtime 用,闭环走既有 `agents_team.persona_registry`(require_pinned)。模块隔离:仅 import 同包 `prompt_policy`(零 agents_team import,base 卡作 opaque text 传入)。
