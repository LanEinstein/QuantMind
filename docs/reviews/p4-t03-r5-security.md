1. `P2` 存储型 XSS 风险。图表 tooltip 通过 HTML 字符串直接拼接 `scenario` 和 `impact` 字段：[frontend/src/components/charts/ExtremeScenarioPie.vue](/home/ps/papers/QuantMind/frontend/src/components/charts/ExtremeScenarioPie.vue#L69) / [frontend/src/components/charts/ExtremeScenarioPie.vue](/home/ps/papers/QuantMind/frontend/src/components/charts/ExtremeScenarioPie.vue#L72)。这两个字段是从 MiroFish 提取结果里的自由文本直接落库/出库的：[backend/mirofish/extractors/extreme_scenarios.py](/home/ps/papers/QuantMind/backend/mirofish/extractors/extreme_scenarios.py#L146) / [backend/mirofish/extractors/extreme_scenarios.py](/home/ps/papers/QuantMind/backend/mirofish/extractors/extreme_scenarios.py#L154) / [backend/mirofish/schemas.py](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L110)。这轮没有看到 `v-html`，但这里本质上是同类问题: 把 LLM/外部内容送进 HTML tooltip。Apache ECharts 官方也明确把 `tooltip.formatter` 回调返回 HTML 视为 XSS 面。建议改成 `renderMode: 'richText'`，或在拼接前对所有动态字段做 HTML escaping。参考: https://echarts.apache.org/handbook/en/best-practices/security , https://echarts.apache.org/handbook/zh/best-practices/security

2. `P2` 生产态 API 失败时会把“旧仿真数据”显示成“新选中的仿真”，属于高风险误导。点击侧边栏时先切换选中态再发请求：[frontend/src/views/Simulation.vue](/home/ps/papers/QuantMind/frontend/src/views/Simulation.vue#L420)；失败后 `fetchById()` 在生产态只把 `status` 设为 `error`，不会清空 `currentSimulation`：[frontend/src/stores/simulation.ts](/home/ps/papers/QuantMind/frontend/src/stores/simulation.ts#L119) / [frontend/src/stores/simulation.ts](/home/ps/papers/QuantMind/frontend/src/stores/simulation.ts#L125)；模板又只要 `currentSimulation` 还在就继续渲染主内容：[frontend/src/views/Simulation.vue](/home/ps/papers/QuantMind/frontend/src/views/Simulation.vue#L93)，同时侧边栏高亮已经切到新项：[frontend/src/views/Simulation.vue](/home/ps/papers/QuantMind/frontend/src/views/Simulation.vue#L69)。结果就是网络/后端异常时，UI 可能显示 A 的内容但高亮 B，且没有显式报错/重试入口。这在运维故障时会直接误导使用者。建议只在请求成功后提交选中态，或失败时清空主内容并展示专门错误态。

其余本轮检查未见 P1/P2/P3:
- `frontend/src` 未发现 `v-html` / `innerHTML`。
- sidebar search 前后端路径未见注入点，后端已对搜索词做 `re.escape()`：[backend/api/simulation.py](/home/ps/papers/QuantMind/backend/api/simulation.py#L95)。
- 未发现硬编码真实凭证；mock key 为 `***masked***`。
- mock fallback 的 `import.meta.env.DEV` 闸门在本改动里是对的。
- `vite.config.ts` 代理是 dev server 配置，未见新增生产暴露面。
- 未见完整 API 响应被 `console.log` 暴露。
- 依赖方面，截至 2026-04-12，锁定版本是 `element-plus 2.13.6`、`dayjs 1.11.20`、`vue-echarts 6.7.3`。`element-plus` 有历史公告 `CVE-2022-27103` 和 `CVE-2025-57665`，但影响范围分别是 `<2.0.6`、`<2.11.0`，不命中当前锁定版本；未查到 `dayjs`/`vue-echarts` 当前锁定版本的直接公开漏洞。参考: https://advisories.gitlab.com/pkg/npm/element-plus/ , https://security.snyk.io/package/npm/dayjs , https://security.snyk.io/package/npm/vue-echarts

NEEDS_FIXES