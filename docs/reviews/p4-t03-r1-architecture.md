- [P1] 播放时钟所需的“时间帧数据契约”只覆盖了情绪图，另外三张图没有可随 round 演化的数据源 — [backend/mirofish/schemas.py#L116](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L116), [frontend/src/types/simulation.ts#L77](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L77), [frontend/src/stores/simulation.ts#L77](/home/ps/papers/QuantMind/frontend/src/stores/simulation.ts#L77), [frontend/src/components/charts/SentimentChart.vue#L39](/home/ps/papers/QuantMind/frontend/src/components/charts/SentimentChart.vue#L39), [frontend/src/components/charts/HiddenVariableMatrix.vue#L50](/home/ps/papers/QuantMind/frontend/src/components/charts/HiddenVariableMatrix.vue#L50), [frontend/src/components/charts/InflectionTimeline.vue#L65](/home/ps/papers/QuantMind/frontend/src/components/charts/InflectionTimeline.vue#L65), [frontend/src/components/charts/ExtremeScenarioPie.vue#L23](/home/ps/papers/QuantMind/frontend/src/components/charts/ExtremeScenarioPie.vue#L23)  
  现在只有 `sentiment_evolution` 是 round-based；`hidden_variables`、`key_inflection_points`、`extreme_scenarios` 仍是整场仿真的静态汇总。store 里也没有任何以 `currentRound` 为输入的派生逻辑，所以实际上只有 `SentimentChart` 能被全局时钟驱动，另外三张图只能显示静态结果。这和“四图联动”的目标是架构级不匹配。  
  修复建议：把后端/前端契约扩成真正的时间帧模型，例如为隐变量/极端场景增加按 round 的权重序列，或至少增加 `visible_from_round`/`peak_round`/`expires_at_round` 之类的时间字段；然后在 store 基于同一个 `currentRound` 生成四张图各自的 frame view model。

- [P2] 后端 canonical schema 与前端 TypeScript optionality 不严格对齐，实际 HTTP 契约绕过了 Pydantic 默认值 — [backend/mirofish/schemas.py#L53](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L53), [backend/mirofish/schemas.py#L76](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L76), [backend/mirofish/schemas.py#L87](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L87), [backend/mirofish/schemas.py#L101](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L101), [backend/mirofish/schemas.py#L120](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L120), [frontend/src/types/simulation.ts#L14](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L14), [frontend/src/types/simulation.ts#L21](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L21), [frontend/src/types/simulation.ts#L30](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L30), [frontend/src/types/simulation.ts#L45](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L45), [frontend/src/types/simulation.ts#L54](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L54), [frontend/src/types/simulation.ts#L72](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L72), [backend/api/simulation.py#L37](/home/ps/papers/QuantMind/backend/api/simulation.py#L37)  
  后端 schema 已经把 enriched 字段定义成“有默认值的正式字段”，理论上 `SimulationResult` 一旦成立，这些键就应该稳定存在；但前端类型把它们全部放成 optional，因为 API 直接把 Mongo 文档原样返回，没有经过 `SimulationResult.model_validate()` 归一化。结果是系统同时存在“Python 内部强契约”和“HTTP 弱契约”两套数据形态，store/组件只能到处写 `??` 和 optional chaining。  
  修复建议：API 出口统一做 schema normalize，再把前端 domain type 收紧为必填；如果必须兼容旧文档，单独定义一个 `SimulationResultWire`，不要让 canonical TS type 长期承担兼容层职责。

- [P1] `direction` 缺失或非法时，极端场景会被静默吞掉，而不是被可视化出来 — [frontend/src/stores/transformers/simulation.ts#L21](/home/ps/papers/QuantMind/frontend/src/stores/transformers/simulation.ts#L21), [frontend/src/components/charts/ExtremeScenarioPie.vue#L33](/home/ps/papers/QuantMind/frontend/src/components/charts/ExtremeScenarioPie.vue#L33), [frontend/src/types/simulation.ts#L54](/home/ps/papers/QuantMind/frontend/src/types/simulation.ts#L54), [backend/mirofish/schemas.py#L98](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L98)  
  `splitScenariosByDirection()` 只接受 `'upside' | 'downside'`，其余值全部丢弃；但 `ExtremeScenarioPie` 的 baseline 又是用 `allScenarios` 总概率计算的。这样一来，legacy 文档里 `direction=""` 的场景会参与总概率，却不会出现在任何扇区里，图表看起来正常，实际丢了数据。  
  修复建议：后端把 `direction` 收紧成 enum 并在 extractor 阶段归一化；前端不要同时接收 `upsideScenarios/downsideScenarios/allScenarios` 三份相关数据，改成只收一个 `scenarios` 源，在组件内部显式生成 `upside/downside/unclassified` 三类。

- [P2] 枚举值只是“注释约定”，没有真正的后端校验；前端还用强制类型断言掩盖异常值 — [backend/mirofish/schemas.py#L38](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L38), [backend/mirofish/schemas.py#L87](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L87), [backend/mirofish/schemas.py#L101](/home/ps/papers/QuantMind/backend/mirofish/schemas.py#L101), [backend/mirofish/extractors/schemas.py#L109](/home/ps/papers/QuantMind/backend/mirofish/extractors/schemas.py#L109), [backend/mirofish/extractors/schemas.py#L124](/home/ps/papers/QuantMind/backend/mirofish/extractors/schemas.py#L124), [frontend/src/stores/transformers/simulation.ts#L74](/home/ps/papers/QuantMind/frontend/src/stores/transformers/simulation.ts#L74)  
  `MomentumShift.direction`、`InflectionPoint.inflection_type`、`ExtremeScenario.direction` 在后端都是普通 `str`，但前端把它们当封闭 union 使用，甚至直接 `as InflectionType`。只要 LLM 输出拼写偏差，后端会照单全收，前端则进入 silent degradation：时间线退回 unknown 样式，极端场景被过滤掉，但没有任何层报警。  
  修复建议：把这些字段改成 `Literal`/`Enum`，在 extractor 输出到 `SimulationResult` 前做显式校验或映射；前端 transformer 改成 runtime guard，而不是类型断言。

- [P2] 播放时钟不会在切换 simulation 时复位，新的数据会继承旧的 round/play 状态 — [frontend/src/views/Simulation.vue#L408](/home/ps/papers/QuantMind/frontend/src/views/Simulation.vue#L408), [frontend/src/views/Simulation.vue#L428](/home/ps/papers/QuantMind/frontend/src/views/Simulation.vue#L428), [frontend/src/composables/usePlayback.ts#L33](/home/ps/papers/QuantMind/frontend/src/composables/usePlayback.ts#L33)  
  `usePlayback()` 只在页面初始化时创建一次，`loadSimulation()` 只替换 store 数据，`reset/play` 只在 `onMounted()` 跑一次。用户如果在 R12 切到另一条仿真，新的页面会直接落在旧 round；如果上一条已经播到结尾，新数据也不会重新开始。  
  修复建议：监听 `currentSimulation?.id` 或 `totalRounds`，每次切换数据源时显式 `playback.reset()`，再按产品策略决定是否自动 `play()`；同时在 `totalRounds` 变小时做 clamp。

结论：MAJOR_CONCERNS。  
核心原因不是单点实现瑕疵，而是“时间演化体验”的数据契约还没有真正建起来，导致四图联动和严格契约这两个 R1 目标目前都没有落稳。